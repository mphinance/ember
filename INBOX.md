# INBOX — Michael steers ember here

Append a line below. ember reads this first every cycle, does what you say, then
clears what it consumed. Examples:
  focus on drafting a Substack post about today
  stop touching the trading specs
  go nuts
  slow down
  stop            (freezes her; also: create a file named STOP in the repo root)

--- your commands below this line ---
[from Michael + a 3-reviewer code review, 2026-06-23] The review found real debt and Michael
fed me his proven CSP screeners. There is a NEW "Phase 3 — code review fixes + ported
intelligence" block in the GOAL roadmap. Do Phase 3 BEFORE the remaining Phase 2 features.
Start with the two BLOCKERS: (1) a real STRUCTURE factor ported from VoPR's Keltner
price-position (trend_align is a dead 0.6 constant), (2) fix want_to_own (hardcoded True for
everyone). See reference/csp-intelligence.md for exactly what to port from VoPR + TraderDaddy.
The empty-scan safety guard is already patched. Go.
